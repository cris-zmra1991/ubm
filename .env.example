# Variables de entorno para la conexión a la base de datos MySQL
# Copia este archivo a .env y ajusta los valores según tu configuración local.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ubm_db
DB_PORT=3306

# Otras variables de entorno para la aplicación (si las hubiera)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
